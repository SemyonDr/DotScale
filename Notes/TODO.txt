------------------------
NOTE: TODO LIST
------------------------

----------------------------------------------------------------------------
OVERALL	--------------------------------------------------------------------
Add Image Chunk processing support

----------------------------------------------------------------------------
CURRENT TASK ---------------------------------------------------------------

Fix another layouts in downscaler
Clean up slice downcaler
Testers for downscaler
Change constructor of downscaler not to rely on ImageBufferInfo

----------------------------------------------------------------------------
CLEANING ---------------------------------------------------------------

make gamma converter dispatcher return reference instead of pointer and add copy/move support to the converter.
Copy/Move support for downscaler and readers/writers
Change image readers/writers to inherit from SliceProcessor for their states.
Change chunk to slice everywhere
Something is not right with ImageBufferInfo at all... It may be redundant.

----------------------------------------------------------------------------
IO	------------------------------------------------------------------------

	Add image metadata support using exiv2 library
	
	PNG Support
		Add palette support.
			Palette reading, storing and writing
			Palette calculating using an algorithm (long term goal just for fun of it)
		Add PNG unknown chunks handler - just save them inside the header and write back as they were. Now decompressor is set to ignore by calling png_set_keep_unknown_chunks().
		Add gamma chunks support.
		Interlaced PNG may not work correctly with chunk reading. Requires investigation.
		
	Make classes PngFileInfo and JpegFileInfo, that will combine headers, metadata and additional information like PNG chunks. In ImageFileInfo replace headers with those objects.
	
	JPEG Support
		Add gamma support

	Add utf8 paths support
	Scan IO objects and change NULL to nullptr wherever possible




----------------------------------------------------------------------------
Processing -----------------------------------------------------------------

Downscaler:
	Add a logic that runs downscaling without parallelism if the slice is too small.
	Add a logic that runs downscaling in iterations if scaling factor is too small. You can run averaging twice for rows and columns separately and make minimal scaling factor 1/65536 which is enough for all practical intents.

Jobs:

----------------------------------------------------------------------------
Testers --------------------------------------------------------------------
	
	
	
	
----------------------------------------------------------------------------
CHECKLISTS	----------------------------------------------------------------

-- IO

PngReader
	ReadFile
	Constructor
	ReadRows
PngWriter
	WriteFile
	Constructor
	WriteRows
JpegReader
	ReadFile
	Constructor
	ReadRows
JpegWriter
	WriteFile
	Constructor
	WriteRows